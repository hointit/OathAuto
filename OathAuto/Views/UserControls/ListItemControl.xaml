<UserControl x:Class="OathAuto.Views.UserControls.ListItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                BorderBrush="Gray"
                BorderThickness="0,0,0,1"
                Padding="10">
            <Grid>
                <TextBlock Text="Inventory Items"
                           FontSize="14"
                           FontWeight="Bold"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Items ListView -->
        <ListView Grid.Row="1"
                  ItemsSource="{Binding Player.InventoryItem}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="80" DisplayMemberBinding="{Binding Id}"/>
                    <GridViewColumn Header="GUID1" Width="100" DisplayMemberBinding="{Binding GUID1}"/>
                    <GridViewColumn Header="GUID2" Width="100" DisplayMemberBinding="{Binding GUID2}"/>
                    <GridViewColumn Header="Name" Width="200" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Delay" Width="100" DisplayMemberBinding="{Binding Delay}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>

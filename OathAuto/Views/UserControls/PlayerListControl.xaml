<UserControl x:Class="OathAuto.Views.UserControls.PlayerListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="800">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <!-- Player ListView -->
    <ListView x:Name="PlayerListView"
              Grid.Row="1"
              ItemsSource="{Binding Players}"
              SelectedItem="{Binding SelectedPlayer, Mode=TwoWay}"
              SizeChanged="PlayerListView_SizeChanged">
      <ListView.View>
        <GridView x:Name="PlayerGridView">
          <GridViewColumn Header="Name" Width="120" DisplayMemberBinding="{Binding DisplayName}"/>
          <GridViewColumn Header="HP %" Width="60" DisplayMemberBinding="{Binding HPPercent, StringFormat={}{0:F1}%}"/>
          <GridViewColumn Header="MP %" Width="60" DisplayMemberBinding="{Binding MPPercent, StringFormat={}{0:F1}%}"/>
          <GridViewColumn Header="Map" DisplayMemberBinding="{Binding MapLocation}"/>
        </GridView>
      </ListView.View>
    </ListView>
  </Grid>
</UserControl>

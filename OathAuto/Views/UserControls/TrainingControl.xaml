<UserControl x:Class="OathAuto.Views.UserControls.TrainingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
  <GroupBox Margin="5">
    <Grid Margin="10">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <CheckBox Content="Training" Grid.Row="0"
          IsChecked="{Binding IsTraining, Mode=TwoWay}"
          FontWeight="Bold"/>

      <StackPanel Grid.Row="1" Orientation="Horizontal" Grid.ColumnSpan="2">
        <CheckBox Content="Tự động thăng cấp khi nhỏ hơn" IsChecked="{Binding Settings.IsAutoUpLevel, Mode=TwoWay}"
          Margin="0,5"/>
        <TextBox Width="60"
         Text="{Binding Settings.MaxLevel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
         VerticalAlignment="Center"
         Margin="10,0"/>

        <CheckBox  Content="Tự dùng x2.5" IsChecked="{Binding Settings.IsAutoUseX2Exp, Mode=TwoWay}" Margin="40,5"/>
      </StackPanel>

      <CheckBox Grid.Row="2" Grid.Column="0" Content="Tự reset khi đủ cấp" IsChecked="{Binding Settings.IsAutoUseResetLevelItem, Mode=TwoWay}" Margin="0,5"/>

      <CheckBox Grid.Row="2" Grid.Column="1" Content="Tự động cộng point" IsChecked="{Binding Settings.IsAutoUseAddPointItem, Mode=TwoWay}" Margin="20,5"/>
        <!-- Get and Fixed postion player -->
      <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0 5">
        <TextBlock VerticalAlignment="Center" Margin="0,0,10,0" Text="Cố định vị trí: "/>
        <TextBlock VerticalAlignment="Center" Margin="0,0,5,0" Text="X:"/>
        <TextBlock VerticalAlignment="Center" Margin="0,0,15,0" Text="{Binding Settings.FixedX, StringFormat={}{0:F0}}"/>
        <TextBlock VerticalAlignment="Center" Margin="0,0,5,0" Text="Y:"/>
        <TextBlock VerticalAlignment="Center" Margin="0,0,15,0" Text="{Binding Settings.FixedY, StringFormat={}{0:F0}}"/>
        <TextBlock VerticalAlignment="Center" Margin="0,0,5,0" Text="Map:"/>
        <TextBlock VerticalAlignment="Center" Margin="0,0,15,0" Text="{Binding Settings.FixedMapName}"/>
        <Button Content="..." Padding="10,2" Command="{Binding GetCurrentPositionCommand}" VerticalAlignment="Center" />
        <Button Content="X" Padding="10,2"   Margin="10,0,15,0" Command="{Binding ClearFixedPositionCommand}" VerticalAlignment="Center" />
      </StackPanel>
      <GroupBox Header="Sử dụng vật phẩm" Grid.Row="4" Grid.ColumnSpan="2">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
          </Grid.RowDefinitions>
          <CheckBox Grid.Row="0" Content="Sử dụng vật phẩm ngay cả khi không training" IsChecked="{Binding Settings.UseItemWithoutTraining, Mode=TwoWay}" Margin="2, 10"/>
          <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" >
            <ItemsControl ItemsSource="{Binding NonEmptyInventoryItems}">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <UniformGrid Columns="2"/>
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" Margin="2" Content="{Binding ItemName}"/>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </ScrollViewer>
        </Grid>
      </GroupBox>
    </Grid>
  </GroupBox>
</UserControl>
